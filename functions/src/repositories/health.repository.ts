// functions/src/repositories/health.repository.ts
// Repositorio (acceso a datos): único lugar que habla con DB/SDK/API externas.
// Ahora es un stub; reemplazar por implementación real cuando haya datos.

export class HealthRepository {
  async getStatus(): Promise<{ ok: true }> {
    return { ok: true };
  }
}


