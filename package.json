{
  "name": "tpp-padi-backend",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n tsc,emul \"npm --prefix functions run build:watch\" \"npm --prefix functions run dev:emul\"",
    "build": "npm --prefix functions run build",
    "test": "npm --prefix functions run test",
    "deploy": "bash deploy.sh"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "prisma": "^5.22.0"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "exceljs": "^4.4.0"
  }
}
